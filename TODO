# v0.1

[X] Add wait_time to YAML config
[X] Ordering scheds by priorities and conflict checking in YAML config
[ ] Profile runner function
[ ] Profile runner state file (readonly)
[ ] systemd service file (which takes yaml templates)
[ ] add-profile (to /etc/scx-adapt), rm-profile, start-profile (from /etc if, not absolute path or not in cwd), print-profile helpers and commands
[ ] Add detailed logging to journal
[ ] Add desktop notifications if VM is available